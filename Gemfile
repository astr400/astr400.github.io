source "https://rubygems.org"

# Pins the same gem set that actions/jekyll-build-pages uses in CI, so a local
# `bundle exec jekyll serve` renders exactly what GitHub publishes. CI does not
# read this file: the action is a container with the gems preinstalled.
gem "github-pages", "~> 232", group: :jekyll_plugins
