---
title: Environment setup
description: Set up your computer, connect to the SFSU virtual machine where MESA runs, and optionally try Docker as a container workflow.
---

Steps through [Git](/setup/git/) are on your computer. The [SFSU virtual machine](/setup/sfsu-vm/) and [Git on that machine](/setup/sfsu-vm-git/) are the remote host where MESA runs. [Docker](/setup/docker/) is optional and only demonstrates a container workflow. Pick a topic below, or skip anything you already have. Day-to-day commands live on the [Use](/use/) index.
